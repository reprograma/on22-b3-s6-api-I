const itens = require("./cafeteria.json")

console.log('Seja bem vinde à *THE COFFE*! Das ruas de Fortaleza para seu computador!')
console.log('Veja a seguir nosso cardápio:')

for(const element of itens){
    console.log(element)
}